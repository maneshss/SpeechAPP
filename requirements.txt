# ── Core UI ──────────────────────────────────────────────────────────────────
streamlit>=1.30
streamlit-mic-recorder>=0.0.8

# ── Speech-to-text ───────────────────────────────────────────────────────────
# Cloud deployment: Groq Whisper API (free tier, no heavy dependencies).
#   Set GROQ_API_KEY in Streamlit Cloud → App settings → Secrets.
#   Get a free key at https://console.groq.com
groq>=0.9.0

# Local / on-device Whisper (also installed on cloud as fallback).
# Requires ffmpeg: brew install ffmpeg (macOS) / apt install ffmpeg (Ubuntu)
# On Streamlit Cloud, Groq API is used first (faster, no RAM cost);
# Whisper is used automatically when running locally without a Groq key.
openai-whisper>=20231117

# ── NLP / phoneme matching ───────────────────────────────────────────────────
rapidfuzz>=3.6
pronouncing>=0.2.0
wordfreq>=3.0
metaphone>=0.6

# ── Data ─────────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
openpyxl>=3.1

# ── Optional TTS fallback ────────────────────────────────────────────────────
gTTS>=2.4
