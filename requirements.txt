# ── Core UI ──────────────────────────────────────────────────────────────────
streamlit>=1.30
streamlit-mic-recorder>=0.0.8

# ── Speech-to-text ───────────────────────────────────────────────────────────
# openai-whisper is excluded from cloud deployment: it depends on PyTorch
# (~700 MB) which exceeds Streamlit Cloud's 1 GB RAM limit.
# The app falls back to a typed-text input automatically when Whisper is absent.
# To enable Whisper locally:
#   pip install openai-whisper
#   brew install ffmpeg   # macOS  |  sudo apt install ffmpeg  # Ubuntu
# openai-whisper>=20231117

# ── NLP / phoneme matching ───────────────────────────────────────────────────
rapidfuzz>=3.6
pronouncing>=0.2.0
wordfreq>=3.0
metaphone>=0.6

# ── Data ─────────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
openpyxl>=3.1

# ── Optional TTS fallback ────────────────────────────────────────────────────
gTTS>=2.4
